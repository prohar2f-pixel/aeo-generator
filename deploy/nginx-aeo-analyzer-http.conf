limit_req_zone $binary_remote_addr zone=aeo_analyzer_ip:10m rate=30r/m;

map $http_origin $aeo_analyzer_cors_origin {
    default "";
    "https://aeo.aiprohar.ru" $http_origin;
    "https://prohar2f-pixel.github.io" $http_origin;
}
